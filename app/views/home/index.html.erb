<div class = "container">
    
    <h1>신이 나를 만들 때</h1>
    <div class="row">
        <% for i in (0..2) %>
            <div class="col-md-4"><%= @list_rand[i] %>
                <% if @list_rand[i] != "으아아아아어아아ㅓ아어ㅏ억" %>
                  을 한스푼
                <% end %>
            </div>
    <% end %>
    </div>
    
    <div class="row">
    <% for i in (0..2) %>
    <div class="col-md-4">
        <% if @list_rand[i] == "으아아아아어아아ㅓ아어ㅏ억" %>
            <%= image_tag 6, width: "250 " %></div>
        <% else  %>
           <%= image_tag @img_rand[i], width: "250 " %></div>
        <% end %>
     
     <% end %>
    </div>
        <a href = "/gg"> 다음 페이지로 </a>
</div>
